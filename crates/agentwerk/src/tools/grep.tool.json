{
  "name": "grep_tool",
  "summary": [
    "Search file contents by substring under the working directory.",
    "Returns file paths, matching lines, or per-file counts depending on `output_mode`. Skips `.git`, `target`, `node_modules`, `vendor` automatically."
  ],
  "constraints": [
    "ALWAYS use this tool for content search. NEVER invoke `grep` or `rg` via bash_tool.",
    "Use the `glob` parameter to scope by file type (`*.rs`, `*.ts`); cheaper than searching the whole tree.",
    "Pair with `read_file_tool` once you have a hit to inspect surrounding code. In content mode, long lines are truncated to ~200 bytes around the match; use `read_file_tool` with `col_offset`/`col_limit` to read the full context."
  ],
  "anti_patterns": [
    "To find files by *name*, not contents — use `glob_tool`.",
    "For open-ended searches that need multiple rounds — delegate to `agent_tool`."
  ],
  "cautions": [
    "IMPORTANT: matches are substring, not regex. Pass the literal text you expect to find.",
    "IMPORTANT: results are capped (`max_results`, default 100); if you suspect more, raise the cap or narrow `pattern` rather than re-running."
  ],
  "output": [
    "Plain text. Format depends on `output_mode`: `files` (default) — one path per line; `content` — match lines use `<path>:<line_no>:<col>: <line>` (1-based column of first match), context-only lines use `<path>:<line_no>: <line>`; `count` — `<path>: <count>` per file.",
    "Empty string when nothing matches."
  ],
  "read_only": true,
  "input_schema": {
    "type": "object",
    "properties": {
      "pattern": {
        "type": "string",
        "description": "Substring to search for. Literal match; not a regex."
      },
      "path": {
        "type": "string",
        "description": "Directory or file to search under (default: `.`)."
      },
      "glob": {
        "type": "string",
        "description": "File-name filter (e.g. `*.rs`, `*.{ts,tsx}`). Applied before content search."
      },
      "output_mode": {
        "type": "string",
        "description": "What to return: `files` (default) — distinct paths that contain the match; `content` — matching lines with file path, line number, and surrounding context; `count` — match count per file."
      },
      "context_lines": {
        "type": "integer",
        "description": "Lines of context before and after each match (default: 0). Only used when `output_mode` is `content`."
      },
      "case_insensitive": {
        "type": "boolean",
        "description": "Match without regard to case (default: false)."
      },
      "max_results": {
        "type": "integer",
        "description": "Maximum number of results to return (default: 100). Applies to whichever unit `output_mode` produces."
      }
    },
    "required": ["pattern"]
  }
}
